from setuptools import setup, find_packages

setup(
    name="typedclient",  # Replace with your package name
    version="0.1.0",
    description="A short description of your package",
    packages=find_packages(),  # Automatically finds submodules in `my_package/`
    python_requires=">=3.3",  # Specify minimum Python version
)
