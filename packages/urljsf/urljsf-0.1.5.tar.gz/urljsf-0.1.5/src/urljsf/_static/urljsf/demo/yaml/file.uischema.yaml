# yaml-language-server: $schema=../../schema/v0/ui.schema.json#

ui:options:
  title: A Task List (from YAML-based UI schema)
$schema:
  ui:options:
    widget: hidden
tasks:
  ui:options:
    copyable: true
  items:
    ui:options:
      urljsf:grid:
        children:
          complete:
            - col-md-3
          task:
            - col-md-5
          deadline:
            - col-md-4
