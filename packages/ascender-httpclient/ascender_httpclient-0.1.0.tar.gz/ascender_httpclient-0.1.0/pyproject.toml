[tool.poetry]
name = "ascender-httpclient"
version = "0.1.0"
description = "ascender-httpclient"
authors = ["AscenderTeam"]
readme = "README.md"
packages = [{ include = "aschttp" }]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.11"
ascender-framework = ">=1.3.0b4"
reactivex = "^4.0.4"
httpx = "^0.28.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
