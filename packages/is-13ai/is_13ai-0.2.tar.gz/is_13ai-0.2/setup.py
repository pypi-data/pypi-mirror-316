from setuptools import setup, find_packages

setup(
    name='is_13ai',
    version='0.2',
    description='A package that uses Azure AI to determine if a number is 13 or not.',
    author='Cyrus Mante',
    packages=find_packages(),
)