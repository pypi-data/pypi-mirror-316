[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "graphfusionai"
version = "0.1.1"
description = "GraphFusionAI: A framework for neural memory networks and knowledge graphs"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "torch",
    "numpy",
    "networkx",
    "pytest",
    "pandas"
]

license = { text = "MIT" }

[project.optional-dependencies]
dev = [
    "pytest",
    "black",
    "mypy"
]

[project.urls]
Homepage = "https://github.com/graphfusionai/graphfusion"
Repository = "https://github.com/graphfusionai/graphfusion"
Documentation = "https://graphfusionai.github.io/graphfusion"