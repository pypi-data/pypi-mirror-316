/**
 * Name: {{host_name | default(plugin_name, true)}}{{ ' - ' ~ scrape_category if scrape_category else ''}}
 * Description: {{host_name | default(plugin_name, true)}}{{ ' - ' ~ scrape_category if scrape_category else ''}}
 * PID: {{pid}} force
 */