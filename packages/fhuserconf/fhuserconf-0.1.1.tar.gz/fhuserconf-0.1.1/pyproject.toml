[tool.poetry]
name = "fhuserconf"
version = "0.1.1"
description = "UserConf is a user configuration management Python library."
authors = ["wsd <wsd@fhit.cn>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
