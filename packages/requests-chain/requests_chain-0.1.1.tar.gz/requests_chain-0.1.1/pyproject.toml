[build-system]
requires = ["setuptools>=42", "wheel", "six"]
build-backend = "setuptools.build_meta"

[project]
name = "requests-chain"
version = "0.1.1"
description = "Adds the ability of using a proxy chains to the python requests module. "
readme = "README.md"
license = { file = "LICENSE" }
authors = [
    { name = "makovez", email = "namosoermejo@gmail.com" }
]
dependencies = ["requests"]  # Add your library dependencies
