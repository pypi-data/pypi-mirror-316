[build-system]
requires = [
    "setuptools>=61.0",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "anchor-terminal"
version = "0.1.2"
description = "A Docker workflow tool"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [
    { name = "Jesper Fjellin", email = "jesperfjellin@gmail.com" }
]

[project.scripts]
anchor = "anchor_terminal.terminal:main"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["anchor_terminal"]
