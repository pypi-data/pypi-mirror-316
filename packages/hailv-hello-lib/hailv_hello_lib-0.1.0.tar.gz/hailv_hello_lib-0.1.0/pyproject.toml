[tool.poetry]
name = "hailv-hello-lib"
version = "0.1.0"
description = "A simple package that prints hello."
authors = ["yangfan_hailv <one.gongjiang@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
