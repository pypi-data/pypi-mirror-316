# A custom superfence for converting tex to svg in markdown

Uses python's `pymdown-extensions` and needs a `texlive-full` installation

See [https://latex-superfence.gitlab.io](https://latex-superfence.gitlab.io)