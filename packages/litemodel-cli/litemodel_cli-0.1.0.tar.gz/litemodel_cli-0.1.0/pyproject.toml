[project]
name = "litemodel-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Joe Pasquantonio", email = "joe.pasquantonio@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "typer>=0.15.1",
]

[project.scripts]
litemodel-cli = "litemodel_cli.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
