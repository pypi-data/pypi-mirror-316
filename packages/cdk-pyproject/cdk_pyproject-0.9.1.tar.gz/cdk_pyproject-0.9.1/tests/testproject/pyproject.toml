[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "testproject"

version = "0.1.0"
description = "Python project for synth test"

requires-python = "==3.11.*"
dependencies = ["peppercorn"]
