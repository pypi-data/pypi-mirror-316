[project]
name = "uv-lambda-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["peppercorn>=0.6", "uv-lambda-1"]

[project.scripts]
uv-lambda-2 = "uv_lambda_2:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
uv-lambda-1 = { workspace = true }
