[project]
name = "uv-lambda-1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
uv-lambda-1 = "uv_lambda_1:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
