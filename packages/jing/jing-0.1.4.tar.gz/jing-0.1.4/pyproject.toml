[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[project]
name = "jing"
version = "0.1.4"
dependencies = [
    "requests",
    'importlib-metadata; python_version<"3.10"',
]
