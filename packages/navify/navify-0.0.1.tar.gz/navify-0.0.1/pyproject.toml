[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "navify"
version = "0.0.1"
dependencies = [
    "spotipy",
    "py-sonic",
    "musicbrainzngs",
    "thefuzz",
    'importlib-metadata; python_version<"3.10"',
]