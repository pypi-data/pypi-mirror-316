from setuptools import setup, find_packages

setup(
    name="Hnatyshakten",
    version="1.0.0",
    description="A package to reverse words with special characters in a text",
    packages=find_packages(),
    classifiers=[
        "Programming Language :: Python :: 3",
        "License :: OSI Approved :: MIT License",
    ],
    python_requires=">=3.6",
)
