name: Manual Tests Run
on:
  workflow_dispatch:

jobs:
  approval:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm manual analysis runner test has been run
        run: echo "Manual analysis runner test has been run"
